stages:
  - deploy

variables:


IDK:
  stage: deploy

  environment:
    name: Prod
    url:

  script:
    - sudo docker-compose build --no-cache && sudo docker-compose up -d





  only:
    - master